#include "WordListGenerator.h"
#include "Occurences.h"

/*Print entire list*/
void printAll(wordList *head);

/*print occurences of a specfic word */
void printSpecific(wordList *head, char * word);

/*basically just calls the function that generates the words and then the function that generates
the occurences*/

void generateALL(DIRECTORY);


